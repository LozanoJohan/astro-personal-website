import { initializeApp } from "firebase/app";

const firebaseConfig = {
  projectId: "astroblogbackend",
  appId: "1:503108142043:web:543572a99bb11b0c129dfe",
};

export const app = initializeApp(firebaseConfig);
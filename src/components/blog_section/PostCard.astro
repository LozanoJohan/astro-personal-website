---
const { title, img, date, url } = Astro.props;
---

<article
    class="bg-gray-900 w-full shadow-xl shadow-black border-gray-600 border-[1px] hover:scale-105 transition-all"
>
    <a href={url}>
        <img src={img} alt={title} class="h-1/2 w-full object-center" />
        <div class="p-4 text-left">
            <h3 class="text-3xl">{title}</h3>
            <p class="text-gray-400">{date}</p>
        </div>
    </a>
</article>

---
const { images } = Astro.props;
---

<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
  {
    images.map((image) => (
        <img
          class="rounded-lg border-gray-800 border inset-0 w-full h-full object-cover object-top shadow-[0_0_15px_rgba(0,0,0,0.5)] hover:scale-[1.03] transition-all"
          src={image}
          alt="Imagen de galería"
        />
    ))
  }
</div>
